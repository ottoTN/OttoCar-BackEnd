{% extends 'base.html.twig' %}

{% block body %}
    <h1>Facture</h1>

    <table>
        <tbody>
            <tr>
                <th>Tariffacture</th>
                <td>{{ facture.tariffacture }}</td>
            </tr>
            <tr>
                <th>Statutfacture</th>
                <td>{{ facture.statutfacture }}</td>
            </tr>
            <tr>
                <th>Piece</th>
                <td>{{ facture.Piece }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('facture_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('facture_edit', { 'id': facture.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
